//ques) to find no of factors of num and count them //
/*#include <stdio.h>
int main(){
    int num,i,count=0;
    printf("enter a num");
    scanf("%d",&num);
    i=1;
    printf("factors of the number=\n") ;
    while (i<=num){
        if (num%i==0)
        {
           printf("%d\n",i);
           count=count+1;
        }
       i++;
      }
      printf("total factors=%d",count);
      return 0;
}*/
